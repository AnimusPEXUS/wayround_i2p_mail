

import wayround_org.utils.socket


def read_next_input_block(sock, stop_flag, limit=20 * (1024**2)):
    # TODO wayround_org.utils.socket....
    return


class SocketListener:
    """
    Class for listening socket connections, working with
    client command  identifiers and routing to controller
    """

    def __init__(self, controller):
        self._controller = controller
        return

    def socket_server_target(self):
        return
